$class=talentum.escenic.plugins.authenticator.MemcachedUserCache
#
# This is the list backing up the AuthenticatorManager.
# MemcachedUserCache uses memcached as a distributed list of users appropriate for multiserver environment.
# HashMapUserCache uses a in memory HashMap for storing users, appropriate for single server environment.
#
# $class=talentum.escenic.plugins.authenticator.HashMapUserCache
 